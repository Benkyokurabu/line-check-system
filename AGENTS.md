# Project Agent Instructions

## Communication Policy

- 原則として本番運用を前提に提案する。
- 検証用の最小構成を提案する場合は、必ず「これは検証用」と明記する。
- 「最低限」「一旦」「仮でOK」と言う前に、本番では何が必要かを説明する。
- 設定追加、DB登録、通知先登録では、まず本番に必要な全件登録を前提に案内する。
- ユーザーが非エンジニア操作をしている場合、画面上の次のクリック位置を具体的に示す。
- 秘密情報はチャットに貼らせず、必要ならローカルファイルや管理画面で扱う。

## Repository Operation Policy

- `自習室予約システム/` は親リポジトリとは別の独立Gitリポジトリである。親は `main`、子は `master` を使用する。
- 勉たん全体の統合設計を再開するときは、最初に `自習室予約システム/統合システム設計メモ.md` を読む。このファイルを統合設計の正本とする。
- 親フォルダから作業していても、`自習室予約システム/` 内の変更は子リポジトリでのみステージ・コミット・pushし、親リポジトリのコミットへ混ぜない。
- 子リポジトリを操作するときは、作業ディレクトリを `自習室予約システム/` に移すか、`git -C 自習室予約システム ...` を使用して対象を明示する。
- このフォルダ内の把握・調査・差分確認など、読み取り操作についてユーザーへの許可確認は不要。
- ファイル変更、DB更新、外部サービス設定変更、送信処理、デプロイなど状態を変える操作は、依頼内容に沿って慎重に行う。
- 別エージェントやユーザーの未コミット変更がある場合、その変更を勝手に戻さない。
- 秘密情報を含む可能性があるファイルは、必要性が明確な場合だけローカルで扱い、チャットには値を出さない。
- 既に本番運用・日常運用で動いているスクリプト、設定、重要データ変換ファイルを編集する前に、必ず `scripts/backup-before-edit.ps1` で変更前バックアップを同じフォルダへ作成する。
- バックアップ名は `元ファイル名.before_<内容>_YYYYMMDD_HHMMSS.拡張子` 形式にし、作成後に差分範囲を確認してから編集する。
## Production Reflection Policy

- コード変更を行った場合は、原則として本番サイト反映まで進める。
- 具体的には、変更後に型チェック・関連lintを実行し、問題がなければ対象ファイルだけをコミットして `main` にpushする。
- VercelがGitHub連携で自動デプロイする前提とし、push後は可能な範囲でデプロイ状態を確認する。
- 未追跡ファイルや依頼外の差分は勝手にコミットしない。
- DB更新、外部サービス設定変更、LINE送信など、コードpush以外の本番状態変更は依頼内容に明確に含まれる場合だけ行う。
