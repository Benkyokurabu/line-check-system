# 次回作業メモ

最終更新: 2026-07-04

## 2026-07-04 作業内容（実装したいこと.txt への対応）

`実装したいこと.txt`（プロジェクト直下、Git管理外）にあった要望3点のうち残り2点を実装した。

1. **一斉LINE送信** — `/contacts` ページに「グループへ一斉送信」パネルを追加。
   生徒に `line_user_aliases.group_name` でグループ名（例: 高3理系）を設定しておくと、
   グループ単位でLINE一斉送信できる（LINE multicast API使用）。
   - **要対応**: 現状どの生徒にもグループが未設定。`/contacts` で各生徒にグループ名を設定する必要あり。
2. **「2人の先生宛メッセージ」対応済みバグ** — 修正済み。
   `complete` API が `teacher_name` を無視して全員分を done にしていたのが原因。
   これに伴い、ダッシュボードに「先生選択」機能を追加（パスワード等の認証はなし、ブラウザのlocalStorageに保存）。
   「完了」ボタンは自分の担当分にしか効かない。

### 本番DBに直接適用したスキーマ変更（`supabase/schema.sql` にも反映済み）

- `line_user_aliases.group_name` カラム追加
- `line_user_aliases.alias_name` の NOT NULL 制約を削除（グループのみ設定できるように）

### 追加/変更したAPI

- `GET /api/admin/teachers` — 先生一覧（ダッシュボードの先生選択用）
- `POST /api/line/broadcast` — グループへの一斉送信
- `POST /api/dashboard/conversations/[userId]/complete` — body に `teacher_name` が必須になった（破壊的変更、フロント側は対応済み）
- `PUT /api/admin/contacts/[userId]` — `group_name` を alias_name と独立して更新可能に

まだコミット・デプロイはしていない（ユーザーの確認待ち）。

---

## 現在の到達点

- LINE Webhook受信 → Supabase `line_messages` 保存（第1段階）完了済み
- AI先生候補判定（第2段階）本番稼働中
  - `GROQ_API_KEY` をVercel環境変数に反映・Redeploy済み
  - 公開API `/api/ai/route-messages` 正常動作確認済み
- 管理ダッシュボード `/dashboard` 追加済み
  - URL: `https://line-check-system.vercel.app/dashboard`
  - 先生タブごとに未対応メッセージを一覧表示
  - 「対応済み」「対応不要」ボタンで各先生が自分のリストを管理
  - 他の先生のリストに影響しない（対応漏れ防止が目的）
- 送信者名取得機能追加済み
  - Webhook受信時にLINE Profile APIから`display_name`を取得して保存
  - 既存レコード補完API `/api/admin/backfill-display-names` 追加済み

## まだ未完了

### 1. 既存レコードの送信者名補完（次回最初にやること）

Vercelのdeployが完了したら以下を実行する（私が実行する）：

```powershell
$token = (Get-Content .env.vercel-import |
  Where-Object { $_ -match '^INTERNAL_API_TOKEN=' } |
  Select-Object -First 1) -replace '^INTERNAL_API_TOKEN=', ''

Invoke-RestMethod `
  -Method Post `
  -Uri "https://line-check-system.vercel.app/api/admin/backfill-display-names" `
  -Headers @{ "x-internal-token" = $token } `
  -ContentType "application/json" `
  -Body '{}'
```

期待レスポンス例：`{ "updated_users": 3 }` など

その後 `/dashboard` を開いて送信者名が表示されていることを確認する。

### 2. Teams通知（後回し）

- `TEAMS_WEBHOOK_URL` は未設定
- 理由: 個人Microsoftアカウントでは組織向けWebhook URL作成不可（AADSTS500200）
- 再開条件: 通知先Teamsに属する職場/学校Microsoftアカウントが使える状態になってから

### 3. AI判定の精度改善（検討中）

現状、誰宛か特定できないメッセージを全先生に信頼度50%で振り分けてしまう。
→ 閾値を上げる（例: 60%未満は無視）か、全員50%のケースを除外するか、要検討。

## 重要なローカルファイル

- `.env.vercel-import` — `GROQ_API_KEY`, `INTERNAL_API_TOKEN`, `CRON_SECRET` を含む。Git管理外。
- `groq.txt` — Git管理外
- `supabase で設定したパスワード.txt` — Supabase接続情報。Git管理外。
- `2026年7月スケジュール.xlsm` — 未追跡（Git管理外）

## Supabase DB接続メモ

動作確認できたPooler:
```
aws-1-ap-northeast-1.pooler.supabase.com:5432
user: postgres.xtrmcfvawwhvaujmblie
database: postgres
```

パスワードは `supabase で設定したパスワード.txt` の先頭行。

## 注意事項

- 秘密情報はチャット・GitHubに貼らない
- `vercel` / `gh` CLIは未インストール。Vercel操作はブラウザで行う。
- Vercel環境変数を変更したら必ずRedeploy（自動では反映されない）

---

## 2026-07-11 作業メモ（次回「続き」で再開）

### 今日の到達点

- `AGENTS.md` を追加し、このフォルダ内の読み取り操作は許可確認不要と明記した。
- Notion連携方針を `Notion連携・塾業務アプリ設計メモ.md` に整理した。
- Notion MCP用の `Codex 読み取り用` コネクトを作成済み。
  - トークンはチャットに貼らず、ローカルの `C:\Users\kudok\.codex\notion_token.txt` に保存済み。
  - Codex側のMCP設定は `C:\Users\kudok\.codex\config.toml` に追加済み。
  - Notion API疎通は確認済み。
  - 次回、Codexを再起動または新セッションで始めると Notion MCP が使える想定。
- 生徒カルテ `/karte` のフィルタを修正した。
  - 学年・担任の選択肢が空にならないようにした。
  - 校舎はクラス一覧Excelの `所属` 列から取得するようにした。
  - 校舎選択肢は `本校` / `南教室` / `両方`。
  - 学校名は `student_roster.school_name` として校舎とは別に保存するようにした。
- Supabase本番DBに `student_roster.school_name` カラムを追加済み。
- `npm run import:roster` を実行し、クラス一覧Excelから再取り込み済み。
  - 321 students / 746 class enrollments。
- `npm run lint` と `npx tsc --noEmit` は成功済み。
- 変更はコミット・push済み。
  - 最新コミット: `eaeb65d fix: derive karte campus from roster excel`

### 次回「続き」と言われたら最初にやること

1. `git status --short --branch` で作業状態を確認する。
2. 必要なら `npm run dev` を起動し、`http://localhost:3000/karte` を確認する。
3. 生徒カルテで以下を確認する。
   - 校舎タブに `本校` / `南教室` / `両方` が出ること。
   - `南教室` で絞ると南教室の生徒だけ出ること。
   - 生徒一覧・詳細で `校舎` と `学校` が別表示になっていること。
4. Codexを再起動済みなら、Notion MCPで共有済みNotionのDB構造棚卸しを始める。

Notion棚卸しの最初の指示:

```text
Notion MCPで共有済みNotionの構造を棚卸ししてください。
まず個別ページ本文や全レコードは読まず、DB名、項目名、項目型、リレーション先、件数、最終更新日だけを整理してください。
そのうえで、塾業務アプリに必要なDBを「必須」「候補」「後回し」「読まない方がよい」に分類してください。
```

### 注意点

- `URL.txt`、`実装したいこと.txt`、`実装できるかどうか.txt` は未追跡のまま。勝手にコミットしない。
- 秘密情報はチャット・GitHubに貼らない。
- `notionアクセストークン.txt` のようなトークンファイルは `.gitignore` 対象。必要なら `C:\Users\kudok\.codex\notion_token.txt` に一本化して、プロジェクト直下には置かない方が安全。
- ローカル開発サーバーを止めた場合、`localhost:3000` は接続拒否になる。必要なら `npm run dev` で再起動する。

---

## 2026-08-06 次回「続き」で表示されない生徒の対応

### 依頼内容

出欠確認画面で「続きを表示」を押しても、表示したい生徒が出てこないケースを調査・修正する。

### 現在の「続き」処理

- 対象画面: `src/app/attendance/page.tsx`
- API: `src/app/api/attendance/candidates/route.ts`
- 画面は候補を最初の20件だけ表示する。
- 「続きを表示」を1回押すごとに、さらに20件を画面へ追加する。
- 「最新状態に更新」で候補を再取得すると、表示位置は先頭20件に戻る。
- `status=pending` では、通常は「今日以降」または「今日作成分」の未確認候補だけを取得する。
- 「過去pendingも表示」を有効にした場合だけ、過去のpending候補も取得する。
- `status=review` の対応済み表示では、対応済み候補は直近指定日数分かつ最大120件。

### 次回の調査手順

1. 画面で対象生徒の名前・LINE表示名・受信日時を確認する。
2. `/api/attendance/candidates?status=pending&include_past=1` の返却データに対象メッセージが存在するか調べる。
3. APIに存在する場合は、画面側の20件表示・並び順・再取得時の表示位置を確認する。
4. APIに存在しない場合は、次を確認する。
   - `attendance_candidates.status` が `pending` / `notion_failed` か。
   - `event_date` が今日以降か。過去分なら「過去pendingも表示」が必要。
   - `line_messages` の受信日時・`line_user_id` が保存されているか。
   - 同じLINEアカウントの候補をまとめる処理で、対象候補が別候補の中に入っていないか。
   - 解析対象外・解析失敗になっていないか。
5. 必要ならDBの候補ID、student_number、LINE user ID、受信日時、statusを確認してから修正する。

### 改修候補

- 「続きを表示」だけに依存せず、件数表示（例: `表示中20件 / 全120件`）を追加する。
- API側の返却件数上限や日付条件で隠れている場合は、画面に「過去分」「取得対象外」「解析待ち」を区別して表示する。
- 対象生徒名で検索できるフィルタを追加し、20件ずつ探さなくてよいようにする。
- 候補の並び順は、原則としてLINE受信日時の新しい順。調査時は候補作成日時との差も確認する。
- 修正後は、通常の今日以降表示、過去pending表示、20件超の候補、同一LINEの兄弟候補、Notion由来の生徒を確認する。

### 注意

- 「生徒が表示されない」原因が、候補一覧のページングではなく、生徒マスタ（`student_roster`）に存在しないこともある。
- Notionの見学中生徒など、Excelにない生徒は `student_number` が `notion:<NotionページID>` になる場合がある。正式な学籍番号が決まった後は、`student_roster`、LINE紐づけ、出欠候補の番号をまとめて移行する必要がある。
- 調査対象の具体例と受信日時が分かれば、まず本番DBを読み取り確認し、データ修正が必要な場合だけバックアップ後に実施する。
