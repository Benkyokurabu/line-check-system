# LINE Check System — 残り作業ガイド

> コード実装とローカル動作確認は **完了済み**。
> ここから先は「GitHubにpush → Vercelデプロイ → Supabase設定 → LINE設定 → 実機テスト」の5ステップ。
> 所要時間の目安：20〜40分。

---

## はじめに：いまどこまで終わっているか

✅ Webhook受信API（`src/app/api/line/webhook/route.ts`）実装済み
✅ 署名検証（HMAC-SHA256）動作確認済み（正→200 / 不正→401）
✅ Supabase `line_messages` への保存・重複防止 動作確認済み
✅ AI先生候補判定API（`/api/ai/route-messages`）実装済み
✅ Teams通知送信API（`/api/notifications/teams/send`）実装済み
✅ Vercel Hobbyでも通る日次Cron設定（`vercel.json`）追加済み
✅ `npm run lint` / `npm run build` 成功
✅ `git init` + 初回コミット済み（秘密情報は未コミット）

これから：**外部サービス（GitHub / Vercel / Supabase / LINE / Teams）の設定**。コードはもう触らなくてOK。

---

## ⚠️ 絶対に守ること

- 次の秘密情報は **チャット・GitHub・スクショに貼らない**。各サービスの入力欄に直接打つ。
  - Supabase Secret key / LINE Channel secret / LINE Channel access token
  - Groq API key / Teams Webhook URL / Internal API token / Cron secret
- `.env.local` と `supabase で設定したパスワード.txt` は **Git管理外**（設定済み。pushしても上がらない）。

---

## ステップ1：GitHubにpush

### 1-1. GitHubで空リポジトリを作成
1. https://github.com/new を開く
2. Repository name：例 `line-check-system`
3. **Public / Private どちらでもOK**（迷ったらPrivate）
4. 「Add a README」「.gitignore」「license」は **すべてチェックしない**（空で作る）
5. 「Create repository」

### 1-2. ローカルからpush
プロジェクトフォルダで以下を実行（`<あなた>` と `<repo>` は自分のものに置き換え）。

```bash
git remote add origin https://github.com/<あなた>/<repo>.git
git branch -M main
git push -u origin main
```

> 認証を求められたら、GitHubのユーザー名と **Personal Access Token**（パスワードではない）を入力。
> トークンが無い場合：GitHub → Settings → Developer settings → Personal access tokens → Tokens(classic) → Generate（`repo` 権限にチェック）。

### 1-3. 確認
GitHubのリポジトリページを開き、`src/app/api/line/webhook/route.ts` が表示されればOK。
**`.env.local` が一覧に無いこと** を必ず確認（あったら即削除＆トークン再発行）。

---

## ステップ2：Vercelにデプロイ

### 2-1. インポート
1. https://vercel.com にGitHubアカウントでログイン
2. 「Add New…」→「Project」
3. ステップ1で作ったリポジトリを「Import」
4. Framework Preset が **Next.js** になっていることを確認（自動検出されるはず）
5. **まだ Deploy を押さない**（先に環境変数を入れる）

### 2-2. 環境変数を登録（ここが最重要）
「Environment Variables」セクションで、以下を追加。
**変数名は `SUPABASE_URL`（`NEXT_PUBLIC_` は付けない）** に注意。

| Name（変数名） | Value（値） |
|---|---|
| `SUPABASE_URL` | `https://xtrmcfvawwhvaujmblie.supabase.co` |
| `SUPABASE_SECRET_KEY` | （自分のSupabase Secret key を貼る） |
| `LINE_CHANNEL_SECRET` | （自分のLINE Channel secret を貼る） |
| `LINE_CHANNEL_ACCESS_TOKEN` | （自分のLINE Channel access token を貼る） |
| `GROQ_API_KEY` | （Groq API key を貼る） |
| `TEAMS_WEBHOOK_URL` | （Teams Workflows Webhook URL を貼る） |
| `INTERNAL_API_TOKEN` | （長いランダム文字列。手動実行用） |
| `CRON_SECRET` | （長いランダム文字列。Vercel Cron用。`INTERNAL_API_TOKEN` と同じでも可） |

- Environment は **Production**（最低限）にチェック。Preview/Developmentにも入れておくと後で楽。
- `.env.local` と同じ値を使う。値が分からなくなったら `.env.local` を開いて確認（チャットには貼らない）。

### 2-3. デプロイ
「Deploy」を押す。完了すると `https://<アプリ名>.vercel.app` のURLが発行される。
→ **このURLをメモ**（報告事項①で使う）。

### 2-4. 確認
`https://<アプリ名>.vercel.app` をブラウザで開いて、Next.jsの初期画面が表示されればOK。

> ※環境変数を後から変更/追加した場合は、Vercelで **再デプロイ（Redeploy）** が必要。

---

## ステップ3：Supabaseの設定

1. Supabase Dashboard の SQL Editor を開く
2. `supabase/schema.sql` の内容をコピーして実行
3. Table Editorで以下のテーブルがあることを確認
   - `line_messages`
   - `line_tasks`
   - `app_settings`
   - `teachers`
   - `ai_message_routes`
   - `teacher_notifications`
4. `teachers` に通知対象の先生を登録
   - `display_name`: AIが返す先生名
   - `aliases`: 表記ゆれ。例 `{"山田先生","山田","Yamada"}`
   - `notification_enabled`: `true`
   - `notification_channel`: `teams`
   - `notification_target`: 先生ごとにTeams URLを分ける場合だけ入力。空なら `TEAMS_WEBHOOK_URL` を使う

## ステップ4：LINE Developers の設定

対象：**Channel ID `2010441579`（Messaging APIチャネル）**

1. https://developers.line.biz/ → 対象チャネル → 「Messaging API設定」タブ
2. **Webhook URL** に次を入力（末尾 `/api/line/webhook` まで必須）：
   ```
   https://<アプリ名>.vercel.app/api/line/webhook
   ```
3. 「**検証 / Verify**」ボタンを押す → **「成功 / Success」** が出ればOK
   → この結果が報告事項②。
4. 「**Webhookの利用**」を **オン**
5. 「**応答メッセージ**」は **オフ推奨**（LINE Official Account Manager側で設定。既存運用に影響がある場合は事前確認）

### 検証が失敗するときのチェックリスト
- [ ] Vercelのデプロイは成功しているか
- [ ] URLに `/api/line/webhook` まで含まれているか
- [ ] Vercelに環境変数4つを登録したか（特に `LINE_CHANNEL_SECRET`）
- [ ] 環境変数を追加した後に **Redeploy** したか
- [ ] 変数名が `SUPABASE_URL`（`NEXT_PUBLIC_`なし）になっているか
- [ ] Vercel → Project → 「Logs / Functions」でエラー内容を確認

---

## ステップ5：実機テスト

1. スマホで対象のLINE公式アカウントを友だち追加し、メッセージを送る：
   ```
   Webhookテストです
   ```
2. Supabase → Table Editor → `line_messages` を開く
3. 新しい行が追加され、以下が入っていればOK（報告事項③）：
   - [ ] `line_user_id` が入っている
   - [ ] `text` に送った本文が入っている
   - [ ] `line_message_id` が入っている
   - [ ] `raw_event` が入っている
   - [ ] `created_at` が入っている
4. **同じ画面でもう一度同じ操作をしても重複行が増えないこと**（別メッセージなら増える）
5. AI判定を手動実行する：
   ```bash
   curl -X POST https://<アプリ名>.vercel.app/api/ai/route-messages \
     -H "content-type: application/json" \
     -H "x-internal-token: <INTERNAL_API_TOKEN>" \
     -d "{\"limit\":10}"
   ```
6. Supabaseで `ai_message_routes` と `teacher_notifications` に行が増えることを確認
7. Teams通知を手動実行する：
   ```bash
   curl -X POST https://<アプリ名>.vercel.app/api/notifications/teams/send \
     -H "content-type: application/json" \
     -H "x-internal-token: <INTERNAL_API_TOKEN>" \
     -d "{\"limit\":10}"
   ```

> 反映されないときは Vercel → Logs でWebhookのエラーを確認。

---

## 完了したら報告する3点

1. **Webhook URL**：`https://<アプリ名>.vercel.app/api/line/webhook`
2. **LINE Webhook検証の結果**：成功 / 失敗（失敗なら上のチェックリストの結果）
3. **Supabase保存確認**：`line_messages` に行が増えたか / 重複しないか

---

## メモ：技術的な前提（つまずいたとき用）

- Supabase接続用の環境変数名は **`SUPABASE_URL`** に統一済み（コード側も対応済み）。
- `line_messages` の本文カラムは **`text`**（`message_text` ではない）。`message_type` / `direction='inbound'` / `received_at` にマップ済み。reply_token等は `raw_event` に丸ごと保存。
- 重複防止は `line_message_id` のunique制約 + `upsert(ignoreDuplicates)` で実装済み。
- AI先生候補判定とTeams通知は実装済み。タスク抽出、自動返信、管理画面は次段階。

---

## この段階ではやらないこと（第2段階以降）

タスク抽出 / `line_tasks` 自動登録 / 自動返信 / LIFF / 管理画面 / 保護者名紐づけ
