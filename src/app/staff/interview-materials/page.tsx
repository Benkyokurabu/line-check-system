import MaterialsDesk from './workspace';
import { pageMetadata } from '@/lib/page-titles';
export const dynamic = 'force-dynamic';
export const metadata = pageMetadata('面談資料を作る');
export default function Page() { return <MaterialsDesk />; }
